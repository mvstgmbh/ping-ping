import { FadeInContainer } from '../../ui/FadeInContainer';
import { MainContainer } from '../../ui/MainContainer';

export const TopPlayers = () => {
  return (
    <MainContainer className="px-4">
      <FadeInContainer>Top Players</FadeInContainer>
    </MainContainer>
  );
};
