/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'image.pexels.com', 'images.unsplash.com', 'images.pexels.com'],
  },
};

module.exports = nextConfig;
